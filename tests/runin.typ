#import "../src/lib.typ": article
#show: article.with(paper: "a4", size: "10")

=== A subsubsection heading

Body text under the subsubsection, long enough to wrap onto a second line of
the paragraph so the spacing is exercised.

==== A run-in heading
Text that follows the run-in paragraph heading on
the very same line, continuing for long enough to wrap.

==== Another one
More text after the second run-in heading.
